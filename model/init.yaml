model_args:
  num_layers: 4
  num_heads: 8
  mlp_ratio: 4
  embed_dim: 64
  qkv_bias: True
  drop_rate: 0.1
  attn_drop_rate: 0.1
  drop_path_rate: 0.1
  max_frames: 200  # 最大序列长度
  in_channels: 3   # 输入通道数(方向余弦)